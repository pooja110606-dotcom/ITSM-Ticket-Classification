# Models Directory

This folder stores trained machine learning models generated during the project.

Examples:
- model.pkl
- model.joblib

These files contain serialized machine learning models that can be used for prediction without retraining the model.
